# Sample Model with API

## Description

This creates a sample Model API that can be sent a reqest from port `4042`.

## Quickstart

To run:

```shell
just run
```

To clean up:

```shell
just clean
```
