version: 2

seeds:

  - name: album_chart
    config:
      enabled: true
      column_types:
        album_id: varchar(22)
        rank_score: varchar(128)
        peak_position: varchar(128)
        week_count: varchar(128)
        date: date
      columns:
        - name: album_id
        - name: rank_score
        - name: peak_position
        - name: week_count
        - name: date
