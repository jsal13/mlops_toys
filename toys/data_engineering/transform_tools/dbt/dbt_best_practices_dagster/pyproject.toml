[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dagster]
module_name = "dbt_best_practices_dagster.definitions"
code_location_name = "dbt_best_practices_dagster"